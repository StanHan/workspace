spring:
  data:
    mongodb:
      uri: mongodb://xinyongjin:eGlueW9uZ2ppbgo=@221.228.75.188:27010/xinyongjin?AutoConnectRetry=true
    repositories:
      enabled: true
    redis:
      database: ${redis.db}
      hostName: ${redis.host}
      port: ${redis.port}
      password: ${redis.pass}
      pool:
        max-active: 8
        max-wait: -1
        max-idle: 8
        min-idle: 0
      timeout: 0
      
config:
  properties: file:/home/b2b2p/ma/config.properties